apiVersion: v1
kind: Service
metadata:
  name: ollama-demo
  namespace: {{.Values.namespace}}
  labels:
    app: ollama-demo
spec:
  type: NodePort
  selector:
    app: ollama-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      nodePort: 30080
